rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Solo usuarios autenticados (login de la app) pueden leer y escribir
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
